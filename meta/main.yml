galaxy_info:
  role_name: debian_common_tasks
  author: gianmarco-mameli
  description: |
    Ansible role to configure Debian based systems common settings and additional disks, compatible with Raspberry Pi Os with a dedicated task

    To do:
      - Better README and documentation
      - Integration tests
      - Gihub release workflows

    Contribution, issues and pull requests are welcome.
  # company: your company (optional)
  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  issue_tracker_url: https://github.com/gianmarco-mameli/ansible_debian_common_tasks/issues

  # Choose a valid license ID from https://spdx.org - some suggested licenses:
  # - BSD-3-Clause (default)
  # - MIT
  # - GPL-2.0-or-later
  # - GPL-3.0-only
  # - Apache-2.0
  # - CC-BY-4.0
  license: MIT

  min_ansible_version: "2.1"

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  #
  # Provide a list of supported platforms, and for each platform a list of versions.
  # If you don't wish to enumerate all versions for a particular platform, use 'all'.
  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  # platforms:
  #   - name: Debian
  #     versions:
  #       - "bullseye"
  #       - "bookworm"
  #   - name: RaspberryPiOS
  #     versions:
  #       - "bullseye"
  #       - "bookworm"

  galaxy_tags:
    - debian
    - raspberrypi
    - os
    - common
    - settings
    # List tags for your role here, one per line. A tag is a keyword that describes
    # and categorizes the role. Users find roles by searching for tags. Be sure to
    # remove the '[]' above, if you add tags to this list.
    #
    # NOTE: A tag is limited to a single word comprised of alphanumeric characters.
    #       Maximum 20 tags per role.

dependencies:
  []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
